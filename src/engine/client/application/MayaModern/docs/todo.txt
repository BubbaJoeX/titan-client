# SwgMayaEditor (MayaModern) - Maya 2026
# Full feature parity with MayaExporter (32-bit legacy) - migrated to 64-bit Maya 2026 with an Importer option.

## Documentation (unified Markdown)
- All docs live under MayaModern/docs/ — start at docs/README.md
- guide.md — commands, translators, deploy, workflows (main day-to-day reference)
- manual.md — long-form editor manual
- ans-format.md — ANS IFF (KFAT/CKAT) technical reference
- Root README.md — build, install, POB command quick table, file-type matrix

## Build Requirements
- Maya 2026 (64-bit) - https://www.autodesk.com/developer-network/platform-technologies/maya
- Maya 2026 Devkit - **Separate download** from Autodesk Feedback Forum (Downloads section). Extract include/ and lib/ into Maya dir or set MAYA_LOCATION to devkit root.
- Visual Studio 2022 (17.8.3+)
- CMake 3.13+
- Configure: cmake -B build -A x64 (Windows 64-bit required for Maya 2026)
- FindMaya checks: MAYA_LOCATION, C:\Program Files\Autodesk\Maya2026, D:\Program Files\Autodesk\Maya2026

## Supported Use Cases (from original todo.txt)
- Modifying hard points/collision on existing objects
- Adding rooms to a structure that already exists
- Changing existing animations (or animation tables?)
- Creating a new structure/ship pob
- Creating a new static object
- Creating a new vehicle
- Creating a new species/skeletal body and animations
- Creating a new wearable/object with occlusion/form modification

## Current Implementation Status

### Standard Import / Export (File > Import / Export via translators)
- [x] msh - Static mesh (MESH/0005); import + export
- [x] mgn - Skeletal mesh (SKMG); import + export (writer: skinned mesh selection)
- [x] skt - Skeleton; import + export
- [x] ans - Animation (KFAT/CKAT); import + export
- [x] flr - Floor import (export N/A)
- [x] sat - Skeletal appearance; import + export
- [x] pob - Portal object; import + export
- [x] lod / lmg - LOD containers; import + export
- [x] dds - DDS texture import

### MSH Import (full)
- [x] SPS geometry (vertices, normals, UVs, indices)
- [x] APPR form: hardpoints (HPTS/HPNT), floor reference (FLOR)
- [x] Multi-shader per mesh, material assignment
- [x] createHardpoints() for static mesh (parent to mesh when no joint map)
- [x] floor_component placeholder when floor reference exists

### MGN Import (SKMG via ImportSkeletalMesh / translator)
- [x] Skin weighting (TWHD/TWDT, createSkinCluster; case-insensitive joint name matching for XFNM)
- [x] Hardpoints (HPTS: STAT + DYN chunks)
- [x] Blend targets (BLTS): names + data preserved for export via swgBlendTargets (and Script Editor log); live blendShape deformer on import disabled (stability)
- [x] Occlusion zones: stored / logged (e.g. swgOcclusionZones); export path supported
- [x] Joint matching (transform names, short names, namespace stripping, LOD suffix stripping)
- [x] Single mesh from all shader groups, material assignment
- [x] Vertex X negation for Maya coordinate parity (game space vs Maya)
- [x] ITL/OITL: valid triangle count matches polygonConnects (degenerate / OOB handling)

### MGN Export
- [x] File > Export Selection (SwgMgn) / skinned mesh pipeline (see translator writer)
- [ ] Parity audit vs MayaExporter for edge-case SKMG versions / optional chunks

### Special Import Commands
- [x] importSkeleton -i path
- [x] importAnimation -i path (SwgAns translator)
- [x] importStaticMesh
- [x] importSkeletalMesh
- [x] importLodMesh
- [x] importPob
- [x] importSat / exportSat
- [x] importStructure (POB + optional FLR + shell MSH/APT/LOD + optional -shader)
- [x] importShader / exportShader
- [ ] Import Components (.cmp)

### POB authoring commands (plugin)
- [x] createPobTemplate, layoutPobCells, addPobPortal, connectPobCells, duplicatePobCell, reportPobPortals, validatePob (see guide.md; PobExtendedCommands)
- [x] pobAuthoring.mel helpers (source from build/Release next to .mll)

### Export (round-trip status)
- [x] exportPob
- [x] exportStaticMesh (msh/apt + shader TGA→DDS)
- [x] exportSkeleton
- [x] exportShader (.sht)
- [x] .mgn / .ans / .sat / .lod / .lmg via File > Export Selection and registered translators (verify per-asset in QA)
- [ ] Export mesh with lightsaber - .lsb generator (not in plugin)
- [ ] Export mgn with ship - cockpit/*.iff bundle (legacy scope)
- [ ] Export structure one-shot - pob/flr/msh/sht/sfp single command (partial: exportPob + exportStaticMesh separately)
- [ ] Reexport command (batch / convenience)

### Commands
- [x] setBaseDir, getDataRootDir
- [x] swgRevertToBindPose
- [x] swgAssetDissector
- [x] swgAnimationBrowser (UI; sources swgAnimationBrowser.mel beside .mll)
- [x] swgPrepareStaticMeshExport

### Optional MEL (not registered as MPxCommand)
- [x] swgStatueize.mel — look-dev materials (source manually; see guide.md)
- [x] swgAnimationBrowser.mel — used by swgAnimationBrowser command

### ANS / animation behavior (recent)
- [x] Rotation keyframes: delta quaternion path aligned with legacy exporter expectations
- [x] Import prep: clear keys + bind pose restore; skip hardpoints (hold_* / swgHardpointParent) from cutKey/dagPose and from anim application

---

## Remaining for Complete 3D Asset Pipeline

### Import Gaps
- [ ] Import Components (.cmp) - component appearance with hardpoints

### Export Gaps / Parity
- [ ] Export mesh with lightsaber (.lsb)
- [ ] Export mgn “with ship” / cockpit IFF bundles
- [ ] Unified “reexport” or batch export UX
- [ ] Writer parity pass: SPS edge cases, SkeletalMeshGeneratorWriter vs in-translator export, SkeletonTemplateWriter

### Export Pipeline Dependencies (legacy MayaExporter naming — audit vs current code)
- [ ] ShaderPrimitiveSetWriter (SPS) — static mesh path may inline equivalent; confirm
- [ ] SkeletalMeshGeneratorWriter — overlap with MgnTranslator::writer; document single path
- [ ] SkeletonTemplateWriter — overlap with exportSkeleton; document
- [ ] StaticMeshBuilder / MeshBuilder — overlap with exportStaticMesh; document

### Commands (MayaExporter parity — not ported)
- [ ] SwgGui - SWG GUI
- [ ] SwgPrep - SWG prep
- [ ] ExportCommand (monolithic legacy)
- [ ] AddAnimationCommand
- [ ] DeleteAnimationCommand
- [ ] VisitAnimationCommand
- [ ] SetAuthorCommand
- [ ] ExporterLog

### Pipeline Workflow
1. **Import** - Load SWG assets (mesh, skeleton, animation, materials)
2. **Edit** - Modify in Maya (hardpoints, blend shapes, geometry, materials)
3. **Export** - Write back to SWG IFF format (translators + export* commands)

Current state: Import is largely complete; static mesh + POB + skeleton + shader + skeletal/anim/SAT/LOD **export paths exist in plugin** (use File > Export Selection and command docs in guide.md). Remaining work: .cmp, lightsaber/ship bundles, batch/reexport UX, legacy command parity, writer audits.

---

## Out of Scope (per original todo.txt)
- Particles - use dedicated editor
- Client effects - see particles
- Lightning - see particles
- Swoosh - see particles
- Textures - use Photoshop, export DDS; [x] DDS import via File > Import (creates file texture node)
- [x] TGA creation: DDS->TGA writes to textureWriteDir (editable); round-trip ready
- [x] TgaToDdsConverter: nvtt_export.exe for TGA->DDS on export (SwgMayaEditor.cfg: nvttExporterPath)
- [x] Shader preservation: swgShaderPath, swgTexturePath on SG for re-export
- Building aysch loader data/customization data
- Client buildout generation
- .pln (planet object)
- Component (add floors with components)
